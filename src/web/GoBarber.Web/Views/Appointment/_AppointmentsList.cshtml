@model List<GoBarber.Web.ViewModels.Appointment.AppointmentViewModel>

@section PageStyle {
    <link rel="stylesheet" href="assets/styles/pages/appointment.css" asp-append-version="true" />
}

<div class="appointments">
    <div class="appointments-container">
        @if (@Model.Count > 0)
        {
            @foreach (var appointment in @Model)
            {
                <div id="@appointment.Id" class="appointment">
                    <div class="appointment-content">
                        <img src="@appointment.User.Avatar" />
                        <div class="appointment-detail">
                            <p>@appointment.User.Name</p>
                            <p>@appointment.Date</p>
                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <div>No Schedules 😭</div>
        }
    </div>
</div>

@section PageScript {
    <script src="assets/scripts/pages/appointment.js"></script>
}

