<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
        xmlns:d="http://xamarin.com/schemas/2014/forms/design"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        x:Class="GoBarber.App.Components.Layout.EntryComponent" x:Name="this" Visual="Material">
    <ContentView.Resources>
        <Style TargetType="Entry">
            <Setter Property="BackgroundColor" Value="Transparent" />
            <Setter Property="TextColor" Value="{StaticResource SecondaryColor}" />
        </Style>
    </ContentView.Resources>
    <ContentView.Content>
        <StackLayout>
            <Entry x:Name="CustomEntry" Focused="Entry_Focused" Unfocused="Entry_Unfocused"
                    IsPassword="{Binding Source={x:Reference this}, Path=EntryIsPassword}"
                    Text="{Binding Source={x:Reference this}, Path=EntryText}" />
        </StackLayout>
    </ContentView.Content>
</ContentView>